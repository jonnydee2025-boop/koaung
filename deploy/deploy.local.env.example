# Copy to deploy/deploy.local.env (gitignored). Used by remote-deploy.ps1 only.

VPS_HOST=your.server.ip
VPS_USER=root
# VPS_PASSWORD=your-ssh-password
# SSH_KEY=C:\Users\You\.ssh\id_ed25519

DOMAIN=bot.yourdomain.com
APP_DIR=/opt/videobot
CERTBOT_EMAIL=you@example.com

# DEPLOY_SKIP_CERTBOT=true
