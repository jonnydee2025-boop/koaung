# VideoBot — nginx site with Let's Encrypt (jexbot.site)
# Generated for production restore; cert paths match certbot layout.

server {
    listen 80;
    listen [::]:80;
    server_name jexbot.site;

    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name jexbot.site;

    ssl_certificate /etc/letsencrypt/live/jexbot.site/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/jexbot.site/privkey.pem;
    include /etc/letsencrypt/options-ssl-nginx.conf;
    ssl_dhparam /etc/letsencrypt/ssl-dhparams.pem;

    root /opt/videobot/admin-panel/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_connect_timeout 60s;
        proxy_send_timeout 3600s;
        proxy_read_timeout 3600s;
    }

    location /docs {
        return 404;
    }
    location /openapi.json {
        return 404;
    }
}
